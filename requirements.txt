# Core data science
pandas>=2.0
numpy>=1.24
pyarrow>=14.0          # per leggere i parquet

# Visualization
matplotlib>=3.7
seaborn>=0.12

# Machine Learning
scikit-learn>=1.3
xgboost>=2.0
optuna>=3.4            # tuning bayesiano

# Deep Learning (notebook 5 — autoencoder anomaly detection)
keras>=3.0             # Keras 3 multi-backend
torch>=2.0             # backend PyTorch (CPU) per Keras 3

# HTTP per Open-Meteo Elevation API
requests>=2.31
truststore>=0.9          # verifica SSL via trust store del SO (proxy/antivirus aziendali)

# Visualizzazione geografica
folium>=0.15           # mappa interattiva (notebook 3)

# Persistenza modelli
joblib>=1.3

# Jupyter (se non già installato nell'ambiente)
jupyter>=1.0
ipykernel>=6.25

keras